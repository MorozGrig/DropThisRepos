@model IEnumerable<DropThisSite.Models.JewelryTip>

@{
    ViewData["Title"] = "Список";
}

<h1>Список</h1>

<p>
    <a asp-action="Create">Создать запись</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameJewelryTip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameJewelryTip)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdJewelryTip">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.IdJewelryTip">Подробнее</a> |
                <a asp-action="Delete" asp-route-id="@item.IdJewelryTip">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
